name: Github to Qase

on:
  push:
    branches:
      - main

jobs:
  install_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: |
          npm install
      - name: Run Playwright tests
        env:
          QASE_TESTOPS_API_TOKEN: ${{ secrets.QASE_TESTOPS_API_TOKEN }} || cb0560c1eab99dc21bc90e7d7931e2d24de4dbd5359b270e5ad2ca79fc504319
          QASE_TESTOPS_PROJECT: ${{ secrets.QASE_TESTOPS_PROJECT }} || CD
          QASE_MODE: "testops"

        run: |
          npx playwright test
          
      - name: List Environment Variables
        run: printenv
